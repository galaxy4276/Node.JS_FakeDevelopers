article.login
    |
    |
    form(action="/auth/login" method="post" autocomplete="off").login__wrapper
        |
        |
        i.fas.fa-times.login__close-btn
        .login__title Login
        //- TODO: 컴포넌트 클릭시에 타이틀 글자 애니메이션 시작, 컴포넌트 밖 클릭시에 애니메이션 중지.
        |
        |
        //- input의 minlegnth, maxlength 는 백엔드와 상의
        .login__input-box
            input(type="text" name="id" minlength="1" maxlength="20" required).login__input
            label.login__input-label Username
            span.login__input-check
        .login__input-box
            input(type="password" name="password" minlength="1" maxlength="20" required).login__input
            label.login__input-label Password
            span.login__input-check
        |
        |
        input(type="submit" value="Sign In").login__login-btn
        a(href="#").login__join-btn Create Account
        |
        |
        div.login__find-password-box
            a(href="#").login__find-password Forgot password?