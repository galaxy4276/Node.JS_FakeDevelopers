doctype html
html(lang="ko")
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        title sendEmail
        link(rel="stylesheet" href="/css/common.css")
        link(rel="stylesheet" href="/css/sendEmail.css")
        script(src="/es5/sendEmail.js")
    body 
        article.find
            |
            |
            .find__overlay
            |
            |
            form(action="/auth/forgot" method="post" autocomplete="off").find__form
                |
                |
                //- TODO: 뒤로가기버튼(로그인 창으로 돌아가기) 추가하기
                .find__form__close-btn
                    <svg xmlns='http://www.w3.org/2000/svg' viewBox='105 105 300 300'><title>닫기</title><path fill='none' stroke='#f5f5f5' stroke-linecap='round' stroke-linejoin='round' stroke-width='40' d='M368 368L144 144M368 144L144 368'/></svg>
                .find__form__title 비밀번호 변경
                |
                |
                fieldset.find__form__fieldset.find__form__fieldset--send-email
                    .find__form__input-box
                        input(type="email" name="email" id="email" minlength="1" maxlength="30" pattern="[a-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-z0-9!#$%&'*+/=?^_`{|}~-]+)*@(?:[a-z0-9](?:[a-z0-9-]*[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]*[a-z0-9])?" required).find__form__input
                        label(for="email").find__form__input-label 가입 시 등록했던 이메일을 입력해주세요
                    button(type="submit").find__form__btn.find__form__btn--send-email 이메일 보내기