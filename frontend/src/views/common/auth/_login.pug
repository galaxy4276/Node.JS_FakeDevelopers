article.login
    |
    |
    .login__overlay
    |
    |
    form(action="/auth/login" method="post" autocomplete="off").login__form
        |
        |
        .login__form__close-btn
            <svg xmlns='http://www.w3.org/2000/svg' viewBox='105 105 300 300'><title>닫기</title><path fill='none' stroke='#f5f5f5' stroke-linecap='round' stroke-linejoin='round' stroke-width='40' d='M368 368L144 144M368 144L144 368'/></svg>
        .login__form__title Login
        //- TODO: 컴포넌트 클릭시에 타이틀 글자 애니메이션 시작, 컴포넌트 밖 클릭시에 애니메이션 중지.
        |
        |
        //- input의 minlegnth, maxlength 는 백엔드와 상의
        .login__form__input-box
            input(type="text" name="id" minlength="1" maxlength="20" required).login__form__input
            label.login__form__input-label Username
            span.login__form__input-check
        .login__form__input-box
            input(type="password" name="password" minlength="1" maxlength="20" required).login__form__input
            label.login__form__input-label Password
            span.login__form__input-check
        |
        |
        input(type="submit" value="Sign In").login__form__login-btn
        a(href="#").login__form__join-btn Create Account
        |
        |
        div.login__form__find-password
            a(href="/auth/find").login__form__find-password-link Forgot password?