article.find
    |
    |
    .find__overlay
    |
    |
    form(action="" method="post" name="findPassword" autocomplete="off").find__form
        |
        |
        //- TODO: 뒤로가기버튼(로그인 창으로 돌아가기) 추가하기
        .find__form__close-btn
            <svg xmlns='http://www.w3.org/2000/svg' viewBox='105 105 300 300'><title>닫기</title><path fill='none' stroke='#f5f5f5' stroke-linecap='round' stroke-linejoin='round' stroke-width='40' d='M368 368L144 144M368 144L144 368'/></svg>
        .find__form__title 비밀번호 찾기
        |
        |
        fieldset.find__form__fieldset.find__form__fieldset--send-email
            .find__form__input-box
                input(type="email" name="email" minlength="1" maxlength="30" pattern="[a-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-z0-9!#$%&'*+/=?^_`{|}~-]+)*@(?:[a-z0-9](?:[a-z0-9-]*[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]*[a-z0-9])?" required).find__form__input
                label.find__form__input-label 가입 시 사용했던 이메일을 입력해주세요
            button(formaction="#" formmethod="post" type="submit" value="").find__form__btn.find__form__btn--send-email 이메일 보내기
        |
        |
        fieldset.find__form__fieldset.find__form__fieldset--check-hash
            |
            |
            .find__form__return-btn
                <svg xmlns='http://www.w3.org/2000/svg' viewBox='105 105 300 300'><title>뒤로가기</title><path fill='none' stroke='#f5f5f5' stroke-linecap='round' stroke-linejoin='round' stroke-width='40' d='M244 400L100 256l144-144M120 256h292'/></svg>
            |
            |        
            .find__form__input-box
                input(type="text" name="emailHash" minlength="1" required).find__form__input
                label.find__form__input-label 입력하신 이메일로 발송된 값을 입력해주세요
            button(formaction="#" formmethod="post" type="submit" value="").find__form__btn.find__form__btn--hash 확인
        |
        |
        fieldset.find__form__fieldset.find__form__fieldset--reset-password
            .find__form__input-box
                input(type="password" name="reset" minlength="1" maxlength="20" required).find__form__input
                label.find__form__input-label 재설정할 비밀번호를 입력해주세요.
                //- TODO: 아래 패스워드 체크에 입력한 값이 위에 입력했던 패스워드와 동일한지 프론트 단에서 검증
            .find__form__input-box
                input(type="password" minlength="1" maxlength="20" required).find__form__input
                label.find__form__input-label 비밀번호 확인
            button(formaction="#" formmethod="post" type="submit" value="").find__form__btn.find__form__btn--reset 비밀번호 재설정